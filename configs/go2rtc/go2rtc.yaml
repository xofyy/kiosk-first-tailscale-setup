api:
  listen: ":1984"
  origin: "*"

rtsp:
  listen: ":8554"
  default_query: "video&audio"

webrtc:
  listen: ":8555"
  candidates:
    - stun:stun.l.google.com:19302

streams:
  # Streams are added dynamically via API
  # Format: rtsp://user:pass@host:port/ISAPI/Streaming/channels/<ID>
